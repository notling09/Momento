/// Die Version von Momento.
///
/// Sie muss mit `version:` in der `pubspec.yaml` uebereinstimmen. Damit die
/// beiden nicht auseinanderlaufen, vergleicht sie ein Test miteinander
/// (siehe `test/app_info_test.dart`).
const momentoVersion = '1.1.0';
